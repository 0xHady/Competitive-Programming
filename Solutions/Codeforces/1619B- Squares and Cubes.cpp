#include <bits/stdc++.h>
#define ll long long
#define thats_what_she_said int main(){ ios::sync_with_stdio(0); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;
thats_what_she_said // x  minutes 
    int T = 1;
    cin >> T;
    while(T--)
    { 
 ll n ; cin >> n;
 cout << (ll) floor(sqrt(n)) + (ll)floor(cbrt(n)) - (ll)floor(sqrt(cbrt(n)));
 cout << endl;
    }
}
